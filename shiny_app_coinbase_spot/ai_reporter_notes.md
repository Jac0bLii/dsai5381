# AI Reporter Notes

I designed the prompt to give the model a compact market snapshot with only the most relevant metrics: latest price, short-term return, lookback return, volatility, volume, and a few recent candles. My first version included a larger JSON payload, but I refined it into a shorter structured prompt so the response would be faster, more consistent, and focused on the exact reporting points I needed.
